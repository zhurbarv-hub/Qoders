# 🚀 Инструкция по установке и запуску

## Требования

- **Python 3.6 или выше**
- SQLite3 (входит в стандартную библиотеку Python)

## Установка Python (если не установлен)

### Windows:

1. Скачайте Python с официального сайта: https://www.python.org/downloads/
2. Запустите установщик
3. **ВАЖНО:** Отметьте галочку "Add Python to PATH"
4. Нажмите "Install Now"
5. После установки откройте командную строку и проверьте:
   ```
   python --version
   ```

### Linux:

```bash
sudo apt update
sudo apt install python3 python3-pip
```

### macOS:

```bash
brew install python3
```

## Быстрый старт

### 1. Проверка установки

Откройте командную строку (PowerShell в Windows) в директории проекта:

```bash
cd d:\QoProj\KKT
```

Проверьте Python:

```bash
python --version
```

### 2. Запуск демонстрации

Для полной демонстрации всех возможностей системы:

```bash
python example_usage.py
```

### 3. Запуск тестов

Для быстрой проверки работоспособности:

```bash
python test_system.py
```

## Использование в своем коде

### Простой пример:

Создайте файл `my_script.py`:

```python
from service_tracker import ServiceTracker
from datetime import datetime, timedelta

# Создаем трекер
with ServiceTracker("my_services.db") as tracker:
    
    # Добавляем сервис
    service_id = tracker.add_service(
        service_name="Мой сервис",
        category="Категория",
        cost=500.00
    )
    
    # Добавляем подписку
    today = datetime.now().date()
    tracker.add_subscription(
        service_id=service_id,
        start_date=today.strftime('%Y-%m-%d'),
        expiration_date=(today + timedelta(days=30)).strftime('%Y-%m-%d'),
        subscription_type="monthly"
    )
    
    # Проверяем истекающие подписки
    expiring = tracker.get_expiring_subscriptions()
    for sub in expiring:
        print(f"⚠️ {sub['service_name']} истекает {sub['expiration_date']}")
```

Запустите:

```bash
python my_script.py
```

## Структура файлов проекта

```
KKT/
├── schema.sql              # SQL схема базы данных
├── service_tracker.py      # Основной модуль (импортируйте этот файл)
├── example_usage.py        # Полная демонстрация функционала
├── test_system.py          # Тесты системы
├── requirements.txt        # Зависимости
├── README.md              # Полная документация
└── INSTALL.md             # Эта инструкция
```

## Интерактивное меню

Для интерактивной работы с системой запустите:

```bash
python example_usage.py
```

Когда появится вопрос о запуске интерактивного меню, введите `y`.

В меню доступны следующие опции:
- Просмотр всех сервисов
- Добавление нового сервиса
- Просмотр активных подписок
- Просмотр истекающих подписок
- Добавление подписки
- Просмотр статистики
- Экспорт данных в JSON

## Работа с базой данных напрямую (SQLite)

Если хотите работать с базой напрямую через SQL:

```bash
# Создайте базу данных
sqlite3 services.db < schema.sql

# Откройте базу для работы
sqlite3 services.db

# В SQLite консоли можете выполнять SQL запросы:
.tables                                    # Показать таблицы
SELECT * FROM services;                    # Получить все сервисы
SELECT * FROM v_active_subscriptions;      # Активные подписки
SELECT * FROM v_expiring_subscriptions;    # Истекающие подписки
.quit                                      # Выход
```

## Автоматизация проверки подписок

Создайте файл `check_daily.py`:

```python
from service_tracker import ServiceTracker

with ServiceTracker() as tracker:
    # Обновляем истекшие подписки
    expired = tracker.check_and_update_expired_subscriptions()
    print(f"Обновлено истекших подписок: {expired}")
    
    # Создаем уведомления
    new_notif = tracker.generate_notifications_for_expiring_subscriptions()
    print(f"Создано новых уведомлений: {new_notif}")
    
    # Получаем неотправленные уведомления
    notifications = tracker.get_pending_notifications()
    for notif in notifications:
        print(f"📧 {notif['message']}")
        # Здесь можно добавить отправку email/Telegram
        tracker.mark_notification_sent(notif['id'])
```

### Настройка автозапуска в Windows:

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте новое задание
3. Триггер: Ежедневно в нужное время
4. Действие: Запустить программу
5. Программа: `python`
6. Аргументы: `d:\QoProj\KKT\check_daily.py`
7. Рабочая папка: `d:\QoProj\KKT`

## Решение проблем

### Python не найден

```
'python' is not recognized as an internal or external command
```

**Решение:** Переустановите Python с галочкой "Add Python to PATH"

### Модуль не найден

```
ModuleNotFoundError: No module named 'service_tracker'
```

**Решение:** Убедитесь, что вы запускаете скрипт из директории, где находится `service_tracker.py`

### Ошибка доступа к базе данных

```
sqlite3.OperationalError: unable to open database file
```

**Решение:** Проверьте права доступа к директории и наличие файла `schema.sql`

## Поддержка

Для получения полной документации см. файл [README.md](README.md)
