# 🔨 Инструкция по компиляции проекта

## Что такое компиляция?

Компиляция преобразует Python скрипты в исполняемые .exe файлы, которые можно запускать на Windows **без установки Python**.

## Подготовка

### 1. Установите Python (если еще не установлен)

Скачайте Python с официального сайта: https://www.python.org/downloads/

**ВАЖНО:** При установке обязательно отметьте галочку **"Add Python to PATH"**!

### 2. Установите PyInstaller

Откройте командную строку (PowerShell) и выполните:

```bash
pip install pyinstaller
```

Или установите все зависимости из requirements.txt:

```bash
cd d:\QoProj\KKT
pip install -r requirements.txt
```

## Автоматическая компиляция

### Способ 1: Запуск batch файла (самый простой)

Просто дважды кликните на файл:
```
compile.bat
```

Скрипт автоматически:
- Установит PyInstaller (если нужно)
- Скомпилирует все файлы
- Создаст .exe файлы в папке `dist/`
- Создаст batch файлы для удобного запуска

### Способ 2: Запуск через командную строку

```bash
cd d:\QoProj\KKT
python build.py
```

## Ручная компиляция

Если автоматическая компиляция не работает, используйте эти команды:

### Компиляция демонстрационного примера:

```bash
pyinstaller --clean --noconfirm --onefile --console ^
  --name=ServiceTracker_Demo ^
  --add-data "schema.sql;." ^
  --hidden-import sqlite3 ^
  example_usage.py
```

### Компиляция тестов:

```bash
pyinstaller --clean --noconfirm --onefile --console ^
  --name=ServiceTracker_Test ^
  --add-data "schema.sql;." ^
  --hidden-import sqlite3 ^
  test_system.py
```

## Результаты компиляции

После успешной компиляции в папке `dist/` появятся:

```
dist/
├── ServiceTracker_Demo.exe   # Демонстрация системы
├── ServiceTracker_Test.exe   # Тестирование
└── schema.sql                # SQL схема (копируется автоматически)
```

## Запуск скомпилированных файлов

### Через batch файлы (рекомендуется):

1. **Запуск_Демонстрации.bat** - запускает демонстрацию
2. **Запуск_Тестов.bat** - запускает тесты

### Напрямую:

1. Откройте папку `dist/`
2. Дважды кликните на нужный .exe файл

## Распространение

Чтобы передать программу на другой компьютер:

1. Скопируйте **всю папку dist/** 
2. На другом компьютере Python **не нужен**
3. Просто запустите .exe файлы

## Решение проблем

### PyInstaller не найден

```
'pyinstaller' is not recognized...
```

**Решение:**
```bash
pip install pyinstaller
```

### Python не найден

```
'python' is not recognized...
```

**Решение:** 
- Переустановите Python с галочкой "Add Python to PATH"
- Или используйте полный путь: `C:\Python39\python.exe build.py`

### Ошибка при компиляции

**Решение:**
1. Обновите PyInstaller: `pip install --upgrade pyinstaller`
2. Попробуйте ручную компиляцию (см. выше)
3. Проверьте, что файл `schema.sql` существует

### .exe файл не запускается

**Возможные причины:**
- Антивирус блокирует (добавьте в исключения)
- Отсутствует schema.sql в папке с .exe
- Недостаточно прав (запустите от администратора)

## Размер .exe файлов

Скомпилированные файлы будут иметь размер ~10-15 МБ каждый, так как включают:
- Интерпретатор Python
- Все необходимые библиотеки
- SQLite
- Ваш код

Это нормально для приложений, созданных с PyInstaller.

## Оптимизация размера

Для уменьшения размера можно:

1. **Использовать --onedir вместо --onefile:**
```bash
pyinstaller --onedir example_usage.py
```
Создаст папку с файлами вместо одного .exe, но размер будет меньше.

2. **Исключить неиспользуемые модули:**
```bash
pyinstaller --exclude-module matplotlib --exclude-module numpy example_usage.py
```

## Дополнительные опции PyInstaller

### Добавить иконку:
```bash
pyinstaller --icon=icon.ico example_usage.py
```

### Скрыть консольное окно (GUI режим):
```bash
pyinstaller --windowed example_usage.py
```

### Добавить версию:
```bash
pyinstaller --version-file=version.txt example_usage.py
```

## Проверка результатов

После компиляции запустите тесты:

```bash
cd dist
ServiceTracker_Test.exe
```

Если все работает корректно, вы увидите сообщение:
```
✨ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО! ✨
```

## Автоматизация сборки

Для регулярной сборки можно создать задачу в планировщике Windows:

1. Откройте **Планировщик заданий**
2. Создайте новое задание
3. Действие: `python d:\QoProj\KKT\build.py`
4. Триггер: по необходимости

---

**Примечание:** Компиляция может занять 1-3 минуты в зависимости от производительности компьютера.
