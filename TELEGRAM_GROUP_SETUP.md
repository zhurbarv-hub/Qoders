# Инструкция по настройке Telegram-группы для уведомлений о обращениях клиентов

## Что было реализовано

Система уведомлений администраторов теперь поддерживает **два режима работы**:

### 1️⃣ Режим группы (рекомендуется)
- Все уведомления отправляются в одну Telegram-группу
- Все администраторы видят обращения в одном месте
- Можно обсуждать обращения в группе
- История доступна всем участникам
- Легко добавлять/удалять администраторов

### 2️⃣ Режим индивидуальных сообщений (резервный)
- Каждому администратору отправляется личное сообщение
- Используется автоматически, если группа не настроена
- Используется как fallback, если отправка в группу не удалась

---

## Настройка группы (пошаговая инструкция)

### Шаг 1: Создание группы в Telegram

1. Откройте Telegram
2. Нажмите **"Новая группа"** (или "New Group")
3. Введите название: **"Обращения клиентов ККТ"**
4. Добавьте всех администраторов системы в группу:
   - Журба Руслан (Telegram ID: 556319278)
   - Елисеев Александр (Telegram ID: 1329276055)
   - Других администраторов, если есть

5. Нажмите **"Создать"**

### Шаг 2: Добавление бота в группу

1. Откройте созданную группу
2. Нажмите на название группы → **"Добавить участников"**
3. Найдите вашего бота (используйте поиск по имени бота)
4. Добавьте бота в группу

### Шаг 3: Настройка прав бота

1. В группе нажмите на название → **"Администраторы"** → **"Добавить администратора"**
2. Выберите вашего бота
3. Включите следующие права:
   - ✅ **Отправка сообщений** (обязательно!)
   - ✅ **Удаление сообщений** (опционально)
   - Остальные права можно отключить
4. Нажмите **"Готово"**

### Шаг 4: Получение Chat ID группы

1. **Отправьте любое сообщение** в группу (например: "Тест")
2. На вашем компьютере запустите:

```bash
python get_group_chat_id.py
```

3. Скрипт покажет Chat ID группы. Пример вывода:

```
📋 Найденные группы:

  Группа: Обращения клиентов ККТ
  Chat ID: -1001234567890
  Тип: supergroup
  ----------------------------------------

💡 Скопируйте Chat ID нужной группы и добавьте в .env файл:
   ADMIN_GROUP_CHAT_ID=<chat_id>
```

4. **Скопируйте Chat ID** (включая знак минус!)

### Шаг 5: Добавление Chat ID в конфигурацию

#### На локальной машине (D:\QoProj\KKT):

1. Откройте файл `.env`
2. Найдите строку `ADMIN_GROUP_CHAT_ID=`
3. Вставьте полученный Chat ID:

```env
ADMIN_GROUP_CHAT_ID=-1001234567890
```

4. Сохраните файл

#### На VDS (сервер 185.185.71.248):

Выполните команду (замените `-1001234567890` на ваш Chat ID):

```bash
ssh root@185.185.71.248 "echo 'ADMIN_GROUP_CHAT_ID=-1001234567890' >> /home/kktapp/kkt-system/.env"
```

Или подключитесь по SSH и отредактируйте файл вручную:

```bash
ssh root@185.185.71.248
nano /home/kktapp/kkt-system/.env
# Добавьте строку: ADMIN_GROUP_CHAT_ID=-1001234567890
# Ctrl+O для сохранения, Ctrl+X для выхода
```

---

## Развертывание обновлений

После настройки Chat ID нужно развернуть обновленный код на сервер:

```bash
# 1. Загрузить обновленные файлы
scp backend/config.py root@185.185.71.248:/home/kktapp/kkt-system/backend/
scp bot/handlers/client_buttons.py root@185.185.71.248:/home/kktapp/kkt-system/bot/handlers/

# 2. Перезапустить бот
ssh root@185.185.71.248 "systemctl restart kkt-bot.service"

# 3. Проверить статус
ssh root@185.185.71.248 "systemctl status kkt-bot.service --no-pager"
```

---

## Проверка работы

### Тестирование уведомлений:

1. Зайдите в бота как клиент
2. Нажмите кнопку **"❓ Помощь"**
3. Создайте тестовое обращение
4. **Проверьте группу** - должно прийти уведомление!

### Что должно произойти:

- ✅ В группу придет сообщение от бота с деталями обращения
- ✅ В логах бота появится запись: `✅ Уведомление об обращении #X отправлено в группу администраторов`

### Если уведомление не пришло:

Проверьте логи бота:

```bash
ssh root@185.185.71.248 "journalctl -u kkt-bot.service -n 50 --no-pager"
```

Возможные проблемы:

1. **"Error 403: Forbidden"** - бот не является администратором группы
   - Решение: Сделайте бота администратором группы (Шаг 3)

2. **"Error 400: Chat not found"** - неверный Chat ID
   - Решение: Проверьте Chat ID (должен начинаться с минуса для групп)

3. **"ADMIN_GROUP_CHAT_ID не настроен"** - переменная не задана
   - Решение: Добавьте переменную в .env (Шаг 5)

---

## Как работает система

```
Клиент создает обращение
         ↓
Бот сохраняет в БД
         ↓
Проверяет: настроена ли группа?
    ↙           ↘
   ДА            НЕТ
    ↓             ↓
Отправка      Отправка
в группу      каждому админу
    ↓             ↓
Успех?        Личные
   НЕТ        сообщения
    ↓
Fallback:
Личные
сообщения
```

---

## Преимущества режима группы

✅ **Централизация** - все обращения в одном месте  
✅ **Совместная работа** - админы видят, кто чем занимается  
✅ **История** - можно вернуться к старым обращениям  
✅ **Простота** - не нужно каждому админу запускать бота  
✅ **Гибкость** - легко добавлять новых администраторов  
✅ **Надежность** - автоматический переход к личным сообщениям при проблемах  

---

## Техническая информация

### Измененные файлы:

1. `backend/config.py` - добавлена переменная `admin_group_chat_id`
2. `bot/handlers/client_buttons.py` - обновлена функция `notify_admins_about_support_request()`
3. `.env.example` - добавлен пример переменной `ADMIN_GROUP_CHAT_ID`

### Новые файлы:

1. `get_group_chat_id.py` - скрипт для получения Chat ID группы

### Переменные окружения:

```env
# Chat ID группы администраторов для уведомлений
# Формат: отрицательное число для групп
ADMIN_GROUP_CHAT_ID=-1001234567890
```

---

## Часто задаваемые вопросы

**Q: Можно ли использовать обычную группу, а не супергруппу?**  
A: Telegram автоматически преобразует обычные группы в супергруппы при добавлении ботов. Chat ID будет начинаться с `-100`.

**Q: Что будет, если удалить переменную ADMIN_GROUP_CHAT_ID?**  
A: Система автоматически переключится на режим индивидуальных сообщений.

**Q: Можно ли отправлять уведомления в несколько групп?**  
A: Текущая реализация поддерживает одну группу. Для нескольких групп нужна доработка.

**Q: Как отключить режим группы и вернуться к личным сообщениям?**  
A: Удалите или закомментируйте строку `ADMIN_GROUP_CHAT_ID` в `.env` файле и перезапустите бота.

**Q: Будут ли работать личные сообщения, если группа настроена?**  
A: Только как fallback - если отправка в группу не удалась.

---

## Поддержка

При возникновении проблем проверьте:

1. ✅ Бот добавлен в группу как администратор
2. ✅ Chat ID правильно скопирован (с минусом)
3. ✅ Переменная ADMIN_GROUP_CHAT_ID добавлена в .env
4. ✅ Бот перезапущен после изменений
5. ✅ В группе было отправлено хотя бы одно сообщение после добавления бота

Логи бота помогут диагностировать проблему:
```bash
ssh root@185.185.71.248 "journalctl -u kkt-bot.service -f"
```
