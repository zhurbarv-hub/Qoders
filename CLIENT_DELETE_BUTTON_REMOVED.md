# Удаление кнопки удаления клиентов

**Дата**: 20 декабря 2025  
**Статус**: ✅ ВЫПОЛНЕНО

## Изменения

Убрана кнопка удаления (корзины) из интерфейса управления клиентами.

### Что изменилось

**Файл**: `web/app/static/js/users.js`

**Было** (строки 123-126):
```javascript
<button class="mdl-button mdl-js-button mdl-button--icon" 
        onclick="deleteUser(${user.id}, '${(user.company_name || user.full_name || '').replace(/'/g, "\\'")}')"
        title="Удалить" style="color: #f44336;" ${!user.is_active ? 'disabled' : ''}>
    <i class="material-icons">delete</i>
</button>
```

**Стало**:
Кнопка удалена. Остались только кнопки активации/деактивации.

### Доступные действия с клиентами

Теперь в столбце "Действия" доступны только:
- ✅ **Активировать** (зелёная кнопка с галочкой) - для неактивных клиентов
- 🚫 **Деактивировать** (оранжевая кнопка с блоком) - для активных клиентов

### Функция deleteUser()

Функция `deleteUser()` (строки 413-481) оставлена в коде, но не используется. 
Может быть удалена при необходимости или оставлена для возможного использования в будущем.

## Развёртывание

### Загруженные файлы
1. ✅ `web/app/static/js/users.js` (22KB) - убрана кнопка удаления
2. ✅ `web/app/static/dashboard.html` - обновлена версия users.js до v=4.12

### Команды
```bash
scp users.js root@185.185.71.248:/home/kktapp/kkt-system/web/app/static/js/
scp dashboard.html root@185.185.71.248:/home/kktapp/kkt-system/web/app/static/
chown kktapp:kktapp /home/kktapp/kkt-system/web/app/static/js/users.js
chown kktapp:kkt-system/web/app/static/dashboard.html
```

## Проверка

1. Откройте https://kkt-box.net/static/dashboard.html
2. Перейдите в раздел "Клиенты"
3. В таблице клиентов в столбце "Действия" должна быть **только** кнопка активации/деактивации
4. Кнопка удаления (красная корзина) должна отсутствовать

## Примечания

- Деактивированные клиенты не удаляются физически из БД
- Для просмотра неактивных клиентов используйте чекбокс "Показать неактивных клиентов"
- Клик по строке таблицы открывает детальную карточку клиента
