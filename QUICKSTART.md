# Быстрый старт - Фаза 1

## ✅ Что уже сделано

1. ✅ Структура папок создана
2. ✅ Файлы конфигурации созданы
3. ✅ База данных инициализирована
4. ✅ Тестовые данные загружены

## ⏳ Что нужно сделать

### Шаг 1: Установить зависимости

Откройте командную строку и выполните:

```bash
cd d:\QoProj\KKT
pip install -r requirements.txt
```

Это займёт 2-5 минут. Дождитесь завершения.

### Шаг 2: Создать .env файл

```bash
python generate_env.py
```

Скрипт автоматически:
- Сгенерирует секретный ключ JWT
- Создаст файл .env с настройками
- Покажет что нужно изменить

### Шаг 3: Получить токен Telegram бота

1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте команду: `/newbot`
4. Введите имя бота: `KKT Services Bot`
5. Введите username: `your_kkt_bot` (должен быть уникальным)
6. Скопируйте полученный токен (формат: `123456789:ABCdef...`)

### Шаг 4: Добавить токен в .env

1. Откройте файл `.env` в блокноте
2. Найдите строку: `TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_FROM_BOTFATHER`
3. Замените `YOUR_BOT_TOKEN_FROM_BOTFATHER` на ваш токен
4. Сохраните файл

### Шаг 5: Проверить установку

```bash
python test_phase1.py
```

Должно быть: **100%** - все проверки пройдены ✅

## 🎉 Поздравляем!

Если все проверки прошли успешно, **Фаза 1 завершена!**

## 📚 Следующие шаги

После завершения Фазы 1 переходите к **Фазе 2: Backend API Development**

Следующие задачи:
1. Создание SQLAlchemy моделей
2. Создание Pydantic схем
3. Реализация API endpoints
4. Настройка аутентификации

---

## ❓ Часто задаваемые вопросы

### Ошибка при установке bcrypt

**Решение:**
```bash
pip install --upgrade pip setuptools wheel
pip install bcrypt
```

### python-dotenv не устанавливается

**Решение:**
```bash
pip install python-dotenv --no-cache-dir
```

### Не могу найти .env файл

Файл `.env` создаётся в корневой папке проекта `d:\QoProj\KKT\.env`

Если не видите - включите отображение скрытых файлов в проводнике.

### База данных пустая

Запустите:
```bash
python database/init_database.py
```

---

**Нужна помощь?** Запустите диагностику:
```bash
python diagnose.py
```
