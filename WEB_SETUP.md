# Быстрый старт веб-интерфейса KKT System

## Проблема: http://localhost:8000 не открывается

### ✅ РЕШЕНИЕ

Веб-интерфейс требует правильной инициализации базы данных с таблицами для клиентов, дедлайнов и пользователей.

---

## 📋 Шаги для запуска

### 1️⃣ Проверить виртуальное окружение

```bash
# Если нет - создать
python -m venv venv_web

# Активировать
venv_web\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### 2️⃣ Инициализировать базу данных

```bash
# Создать таблицы + добавить администратора
venv_web\Scripts\python web\init_web_db.py

# Добавить тестовых клиентов (опционально)
venv_web\Scripts\python web\add_test_data.py
```

**Результат:**
```
✅ Таблицы созданы (7):
   - clients
   - contacts  
   - deadline_types
   - deadlines
   - notification_logs
   - users
   - web_users

✅ Создан администратор:
   Логин: admin
   Пароль: admin123
```

### 3️⃣ Запустить веб-сервер

```bash
# Простой способ - использовать готовый скрипт
start_web.bat

# ИЛИ запустить вручную
venv_web\Scripts\python -m uvicorn web.app.main:app --host 127.0.0.1 --port 8000 --reload
```

### 4️⃣ Открыть в браузере

**URL:** http://localhost:8000

**Вход:**
- Логин: `admin`
- Пароль: `admin123`

---

## 🔧 Диагностика проблем

### Проверить статус базы данных:

```bash
venv_web\Scripts\python check_db.py
```

Должно показать:
```
✅ База данных найдена: database/kkt_services.db

📊 Таблицы в БД (7):
   - clients: 3 записей
   - deadline_types: 4 записей
   - deadlines: 10 записей
   - web_users: 1 записей
   ...
```

### Проверить, занят ли порт 8000:

```powershell
netstat -ano | findstr :8000
```

Если порт занят другим процессом:
```powershell
# Убить все Python процессы
Stop-Process -Name python -Force

# Подождать 5 секунд и запустить снова
start_web.bat
```

### Проверить логи сервера:

При запуске `start_web.bat` должно появиться:
```
INFO: Application startup complete.
INFO: Uvicorn running on http://127.0.0.1:8000
```

Если видите ошибку `no such table: clients` - значит БД не инициализирована, выполните шаг 2️⃣.

---

## 📊 Структура базы данных

**Путь:** `database/kkt_services.db`

**Основные таблицы:**
- `clients` - Клиенты (организации)
- `deadlines` - Сроки истечения услуг
- `deadline_types` - Типы услуг (ККТ, ОФД, ФН и т.д.)
- `web_users` - Пользователи веб-интерфейса
- `contacts` - Telegram контакты
- `notification_logs` - История уведомлений

---

## 🎯 API Документация

После запуска сервера доступна по адресам:
- **Swagger UI:** http://localhost:8000/api/docs
- **ReDoc:** http://localhost:8000/api/redoc

---

## 💡 Полезные команды

```bash
# Посмотреть все таблицы и записи
venv_web\Scripts\python check_db.py

# Пересоздать БД (удалить старую + создать новую)
del database\kkt_services.db
venv_web\Scripts\python web\init_web_db.py
venv_web\Scripts\python web\add_test_data.py

# Проверить подключение через curl (PowerShell)
Invoke-WebRequest -Uri "http://localhost:8000/" | Select-Object StatusCode

# Открыть в браузере через командную строку
Start-Process "http://localhost:8000"
```

---

## 📝 Что было исправлено

1. ✅ Исправлен путь к базе данных в `web/app/database.py`
2. ✅ Создан скрипт инициализации БД `web/init_web_db.py`
3. ✅ Создан скрипт добавления тестовых данных `web/add_test_data.py`
4. ✅ Создан скрипт проверки БД `check_db.py`
5. ✅ Убиты конфликтующие процессы на порту 8000
6. ✅ Запущен правильный FastAPI app: `web.app.main:app`
7. ✅ Добавлена таблица `web_users` для аутентификации

---

**Дата обновления:** 12.12.2025  
**Версия:** 1.0
