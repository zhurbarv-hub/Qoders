# Автоматическое создание дедлайнов для кассовых аппаратов

## ✅ Реализация завершена: 14 декабря 2025

---

## 📋 Описание функциональности

При создании или редактировании кассового аппарата (ККТ) в карточке клиента автоматически создаются соответствующие дедлайны на основе дат истечения ФН и ОФД.

### Логика работы

**При СОЗДАНИИ кассы:**
- Если заполнено поле **"Дата окончания ФН"** → создается дедлайн типа **"Замена ФН"**
- Если заполнено поле **"дата окончания ОФД"** → создается дедлайн типа **"Продление договора ОФД"**

**При РЕДАКТИРОВАНИИ кассы:**
- **Дата изменилась** → существующий дедлайн обновляется с новой датой
- **Дата удалена** → существующий дедлайн отменяется (status = 'cancelled')
- **Дата добавлена** → создается новый дедлайн

---

## 🔧 Технические детали

### Системные типы дедлайнов

Два типа дедлайнов являются **системными** (is_system = true) и используются для автоматического создания:

| ID | Название типа          | is_system | Назначение |
|----|------------------------|-----------|------------|
| 6  | Замена ФН              | true      | Создается из поля "Дата окончания ФН" |
| 7  | Продление договора ОФД | true      | Создается из поля "дата окончания ОФД" |

**Важно:**
- Эти типы **неудаляемые** через UI
- Остальные типы дедлайнов - **пользовательские** (is_system = false)
- Пользовательские типы можно свободно создавать, редактировать и удалять

### Структура дедлайна

Автоматически созданный дедлайн содержит:
- `client_id` - ID клиента (владельца кассы)
- `cash_register_id` - ID кассового аппарата
- `deadline_type_id` - ID типа дедлайна (6 или 7)
- `expiration_date` - Дата истечения (из соответствующего поля кассы)
- `status` - Статус ('active' при создании)
- `notes` - Автоматическая запись о создании/обновлении с датой и временем

### Пример автоматической заметки

```
Автоматически создано из карточки ККТ 'Atol 91F' (2025-12-14 15:30)
```

При обновлении:
```
Автоматически создано из карточки ККТ 'Atol 91F' (2025-12-14 15:30)
Обновлено: 2025-12-20 → 2025-12-25 (2025-12-14 16:45)
```

---

## 📁 Измененные файлы

### 1. База данных
**Файл:** `update_system_types.sql`

```sql
UPDATE deadline_types SET is_system = true WHERE type_name = 'Замена ФН';
UPDATE deadline_types SET is_system = true WHERE type_name = 'Продление договора ОФД';
```

### 2. API кассовых аппаратов
**Файл:** `web/app/api/cash_registers.py`

**Исправления:**
- ✅ Исправлены имена полей PostgreSQL (fn_expiry_date, ofd_expiry_date)
- ✅ Исправлен client_id вместо user_id
- ✅ Исправлен model вместо register_name
- ✅ Исправлен factory_number вместо serial_number
- ✅ Добавлен try-catch для безопасной синхронизации дедлайнов

**Создание кассы (строки 154-166):**
```python
try:
    deadline_service = CashRegisterDeadlineService(db)
    deadline_service.sync_deadlines_on_create(
        cash_register_id=register.id,
        user_id=register.client_id,
        register_name=data.model or f"Касса #{register.id}",
        fn_replacement_date=data.fn_expiry_date,
        ofd_renewal_date=data.ofd_expiry_date
    )
except Exception as e:
    print(f"Предупреждение: не удалось создать дедлайны: {e}")
```

**Редактирование кассы (строки 226-239):**
```python
try:
    deadline_service = CashRegisterDeadlineService(db)
    deadline_service.sync_deadlines_on_update(
        cash_register_id=register.id,
        user_id=register.client_id,
        register_name=register.model or f"Касса #{register.id}",
        old_fn_date=old_fn_date,
        new_fn_date=register.fn_expiry_date,
        old_ofd_date=old_ofd_date,
        new_ofd_date=register.ofd_expiry_date
    )
except Exception as e:
    print(f"Предупреждение: не удалось синхронизировать дедлайны: {e}")
```

### 3. Сервис автоматических дедлайнов
**Файл:** `web/app/services/cash_register_deadline_service.py`

**Ранее исправлено:**
- ✅ client_id вместо user_id в создании Deadline (строка 119)
- ✅ Правильный импорт DeadlineType

**Методы сервиса:**

1. **`sync_deadlines_on_create()`** - создание дедлайнов при создании кассы
2. **`sync_deadlines_on_update()`** - синхронизация при редактировании
3. **`create_deadline_for_register()`** - создание нового дедлайна
4. **`update_deadline_for_register()`** - обновление существующего
5. **`cancel_deadline_for_register()`** - отмена дедлайна

---

## 🧪 Тестирование

### Проверка в базе данных

**Проверить системные типы:**
```sql
SELECT id, type_name, is_system, is_active 
FROM deadline_types 
WHERE type_name IN ('Замена ФН', 'Продление договора ОФД');
```

**Результат:**
```
 id |       type_name        | is_system | is_active
----+------------------------+-----------+-----------
  6 | Замена ФН              | t         | t
  7 | Продление договора ОФД | t         | t
```

**Проверить дедлайны по кассам:**
```sql
SELECT 
    d.id, 
    d.client_id, 
    d.cash_register_id, 
    dt.type_name, 
    d.expiration_date, 
    d.status 
FROM deadlines d 
JOIN deadline_types dt ON d.deadline_type_id = dt.id 
WHERE d.cash_register_id IS NOT NULL 
ORDER BY d.id;
```

### Сценарий тестирования в UI

1. **Создание кассы с датами:**
   - Откройте карточку клиента
   - Нажмите "Добавить кассу"
   - Заполните поля:
     - Модель: "Atol 91F"
     - Дата окончания ФН: 2025-12-31
     - дата окончания ОФД: 2026-01-15
   - Сохраните
   - **Ожидаемый результат:** Создано 2 дедлайна

2. **Редактирование дат:**
   - Откройте созданную кассу
   - Измените "Дата окончания ФН" на 2026-01-31
   - Сохраните
   - **Ожидаемый результат:** Дедлайн обновлен с новой датой

3. **Удаление даты:**
   - Откройте кассу
   - Очистите поле "дата окончания ОФД"
   - Сохраните
   - **Ожидаемый результат:** Дедлайн ОФД отменен (status = cancelled)

---

## 🚀 Статус развертывания

### ✅ Выполнено на VDS (185.185.71.248)

- ✅ Типы дедлайнов обновлены в БД PostgreSQL
- ✅ Код API обновлен и развернут
- ✅ Веб-сервис перезапущен (PID: 48899)
- ✅ Сервис работает на порту 8000
- ✅ Доступ через Nginx на порту 8080

### Проверка работы сервиса

```bash
# Проверка процесса
ps aux | grep uvicorn

# Результат:
# kktapp  48899  4.4 304524 89584 ? Ssl 14:42 uvicorn web.app.main:app
```

### Логи запуска

```
Dec 14 14:42:12 uvicorn[48899]: 🚀 FastAPI приложение запущено!
Dec 14 14:42:12 uvicorn[48899]: 📊 База данных: postgresql://kkt_user:***@localhost:5432/kkt_production
Dec 14 14:42:12 uvicorn[48899]: 📡 API endpoints:
  - /api/cash-registers (Cash Registers)
  - /api/deadlines (Deadlines)
```

---

## 📝 Примечания

### Безопасность

- Сервис обернут в try-catch - ошибки создания дедлайнов не блокируют создание кассы
- Логируются все ошибки для отладки
- Транзакции обеспечивают целостность данных

### Производительность

- Дедлайны создаются в рамках одной транзакции с кассой
- Используется db.flush() для получения ID до commit
- Кеширование ID типов дедлайнов в сервисе

### Будущие улучшения

1. Добавить email-уведомления при приближении дедлайнов
2. Настраиваемые интервалы уведомлений
3. Массовое обновление дедлайнов при изменении типов
4. История изменений дедлайнов

---

## 👤 Автор

Реализовано: 14 декабря 2025  
Разработчик: AI Assistant  
Проект: KKT Services Management System
