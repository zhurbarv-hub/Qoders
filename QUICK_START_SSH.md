# Быстрый запуск SSH туннеля для работы с VDS

## ✅ Статус: SSH туннель работает!

Туннель успешно создан и проброшен на `http://localhost:8080`

## 🚀 Как запустить туннель

### Способ 1: Фоновый режим (рекомендуется)

```powershell
.\start_ssh_tunnel_background.ps1
```

Откроется новое окно PowerShell с SSH подключением. **Не закрывайте это окно!**

### Способ 2: В текущем окне

```powershell
.\start_ssh_tunnel.ps1
```

Туннель будет работать в текущем окне. Нажмите `Ctrl+C` для остановки.

## 🌐 Доступ к веб-интерфейсу

После запуска туннеля откройте браузер:

```
http://localhost:8080
```

**Учётные данные:**
- Email: `admin@kkt.local`
- Пароль: `admin123`

## 📋 Проверка работы карточки клиента

1. Войдите в систему
2. Перейдите в раздел **"Пользователи"** или **"Клиенты"**
3. Кликните на строку клиента (должна открыться карточка)
4. В карточке должны отображаться:
   - ✅ Информация о клиенте (контакты, ИНН, адрес)
   - ✅ Статус Telegram подключения
   - ✅ Список кассовых аппаратов
   - ✅ Текущие дедлайны
   - ✅ Кнопки для добавления кассы и дедлайнов

## 🛑 Остановка туннеля

```powershell
.\stop_ssh_tunnel.ps1
```

Или просто закройте окно SSH (если запускали в фоновом режиме).

## ⚠️ Важные замечания

1. **Туннель должен быть активен** для работы с веб-интерфейсом
2. **Не закрывайте окно SSH** (при фоновом режиме)
3. Если туннель отключился - просто запустите его заново
4. Проверить статус туннеля: `Get-NetTCPConnection -LocalPort 8080`

## 🔧 Устранение проблем

### Порт 8080 занят

```powershell
# Узнать, что занимает порт
Get-NetTCPConnection -LocalPort 8080 | Select-Object OwningProcess | Get-Process

# Остановить туннель
.\stop_ssh_tunnel.ps1
```

### Браузер не открывает страницу

1. Проверьте, что туннель активен (не закрыто окно SSH)
2. Попробуйте обновить страницу (F5)
3. Очистите кеш браузера (Ctrl+Shift+Delete)
4. Попробуйте другой браузер

### SSH запрашивает пароль каждый раз

Настройте SSH ключ для автоматического входа (см. `SSH_TUNNEL_README.md`).

## 📊 Тестирование API

Проверить работу API через туннель:

```powershell
.\test_local_api.ps1
```

Должно вывести:
- ✅ Token received
- ✅ Found clients
- ✅ Full response with JSON data
- ✅ Field validation passed

## 🎯 Следующие шаги

После запуска туннеля и входа в систему:

1. **Проверьте карточку клиента** - все функции должны работать
2. **Попробуйте создать кассу** - форма должна открываться
3. **Добавьте дедлайн** - создание должно работать
4. **Проверьте отображение дедлайнов** - списки должны загружаться

Если что-то не работает - проверьте консоль браузера (F12 → Console) на наличие ошибок JavaScript.

---

**Статус:** ✅ Готово к использованию  
**Дата:** 2025-12-14  
**VDS:** 185.185.71.248:8080  
**Локальный доступ:** http://localhost:8080
