# РЕШЕНИЕ ПРОБЛЕМЫ: Типы услуг не удаляются и не редактируются

## Проблема
Пользователи не могли редактировать или удалять типы услуг (deadline types) через веб-интерфейс.

## Причина
**Несоответствие прав доступа между фронтендом и бэкендом:**

1. **Бэкенд (API)** - строгая проверка роли:
   ```python
   # web/app/api/deadline_types.py, строки 117 и 162
   if current_user.get('role') != 'admin':
       raise HTTPException(status_code=403, detail="Только администратор может...")
   ```
   - Разрешено только для роли `admin`

2. **Фронтенд (JavaScript)** - мягкая проверка:
   ```javascript
   // web/app/static/js/deadline-types.js, строка 49 (ДО исправления)
   const isAdmin = ['admin', 'manager'].includes(user.role);
   ```
   - Кнопки показывались для `admin` И `manager`

**Результат:**
- Manager видел кнопки редактирования/удаления
- При попытке использовать - получал ошибку 403 Forbidden
- Но JavaScript показывал только общее "Ошибка удаления" без деталей

## Решение

### 1. Исправлена проверка роли в фронтенде
```javascript
// web/app/static/js/deadline-types.js, строка 49
const isAdmin = user.role === 'admin';  // Только admin может редактировать и удалять
```

### 2. Улучшена обработка ошибок
```javascript
// Показываем конкретное сообщение от сервера
if (!response.ok) {
    const errorData = await response.json().catch(() => ({}));
    const errorMessage = errorData.detail || 'Ошибка удаления типа услуги';
    throw new Error(errorMessage);
}
```

### 3. Добавлена кнопка редактирования
Теперь в таблице есть две кнопки:
- 🔵 Редактировать (edit icon)
- 🔴 Удалить (delete icon)

Раньше можно было редактировать только кликом на строку, что не было интуитивно понятно.

## Изменённые файлы
- `web/app/static/js/deadline-types.js` (строки 49, 89-93, 211-213, 299)

## Тестирование
Созданы скрипты для проверки:
1. `check_deadline_types.py` - просмотр всех типов в БД
2. `test_deadline_types_access.py` - тестирование прав доступа через API

## Результат
✅ Теперь только администраторы (`admin`) видят и могут использовать кнопки управления типами услуг
✅ При ошибках показываются конкретные сообщения от сервера
✅ Интерфейс стал более интуитивным с явными кнопками редактирования и удаления
