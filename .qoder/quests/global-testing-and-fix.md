# Глобальное тестирование и исправление системы ККТ

## Обзор задачи

Данная задача направлена на комплексное тестирование и устранение выявленных дефектов в системе управления сроками истечения услуг ККТ. Система состоит из трёх ключевых компонентов, каждый из которых требует детальной проверки и исправления.

### Цели задачи

Провести двухэтапное тестирование системы ККТ с последующим устранением обнаруженных проблем:

- **Этап 1**: Тестирование и исправление фронтэнд-части (Web Interface) и библиотеки CoreUI
- **Этап 2**: Диагностика и исправление проблем запуска и работы Telegram-бота

### Компоненты системы

| Компонент | Технология | Порт | Назначение |
|-----------|-----------|------|-----------|
| Backend API | FastAPI + SQLAlchemy + SQLite | 8000 | RESTful API для управления данными |
| Web Interface | HTML + JavaScript + Material Design Lite | 3000 | Веб-интерфейс для управления клиентами и дедлайнами |
| Telegram Bot | aiogram 3.2+ | - | Интерфейс уведомлений и управления через Telegram |
| Scheduler | APScheduler | - | Фоновые задачи проверки дедлайнов |

## Этап 1: Тестирование и исправление фронтэнд-части

### Область тестирования

#### 1.1 Веб-интерфейс (Dashboard)

##### Аутентификация и авторизация

| Проверяемая функция | Ожидаемое поведение | Критерии успеха |
|---------------------|---------------------|-----------------|
| Вход в систему | Пользователь вводит логин/пароль, получает JWT токен | Успешная авторизация, перенаправление на дашборд |
| Сохранение токена | Токен сохраняется в localStorage | Токен доступен при перезагрузке страницы |
| Автовыход при истечении токена | При 401 ответе пользователь перенаправляется на login.html | Корректная обработка истёкших токенов |
| Выход из системы | Кнопка выхода очищает токен и перенаправляет на логин | Полная очистка данных сессии |

##### Панель статистики (Dashboard Cards)

| Карточка | Поле данных | Источник данных API | Проблема для проверки |
|----------|-------------|---------------------|----------------------|
| Всего клиентов | total_clients | `/api/dashboard/stats` | Отображение некорректного значения или дефис |
| Активных клиентов | active_clients | `/api/dashboard/stats` | Несоответствие с данными БД |
| Всего дедлайнов | total_deadlines | `/api/dashboard/stats` | Неверный подсчёт активных дедлайнов |
| Истекает скоро | status_red + status_yellow | `/api/dashboard/stats` | Неправильная логика суммирования |
| Просрочено | status_expired | `/api/dashboard/stats` | Некорректный расчёт просроченных |

##### Графики (Charts)

**График распределения по статусам (statusChart)**

- Тип диаграммы: Doughnut (кольцевая)
- Сегменты:
  - Зелёный (> 14 дней) - status_green
  - Жёлтый (7-14 дней) - status_yellow
  - Красный (0-7 дней) - status_red
  - Серый (просрочено) - status_expired

**Проверяемые аспекты**:
- Корректность данных из API
- Соответствие цветов статусам
- Отображение легенды и подписей
- Отсутствие ошибок при нулевых значениях

**График дедлайнов по типам (typeChart)**

- Тип диаграммы: Bar (столбчатая)
- Данные: Распределение дедлайнов по типам услуг
- Текущая проблема: Передаётся пустой массив `[]`

**Требуется проверить**:
- Почему данные не загружаются из API
- Необходимо ли создать отдельный endpoint для статистики по типам
- Корректность отрисовки при наличии данных

##### Таблица срочных дедлайнов

**Текущая проблема**: Таблица всегда показывает "Нет срочных дедлайнов"

**Проверяемые аспекты**:

| Аспект | Описание проблемы | Метод проверки |
|--------|------------------|----------------|
| Источник данных | Функция `renderUrgentDeadlines([])` получает пустой массив | Определить, откуда должны загружаться данные |
| Endpoint API | Отсутствует вызов API для получения срочных дедлайнов | Найти или создать endpoint типа `/api/deadlines/urgent` |
| Фильтрация | Логика определения "срочности" (days_remaining <= 14) | Проверить бэкенд-логику расчёта |
| Форматирование | Корректность отображения даты, статуса, цвета | Валидация при наличии тестовых данных |

#### 1.2 CoreUI / Material Design Lite

##### Компоненты для проверки

| Компонент MDL | Использование | Проверяемая проблема |
|---------------|---------------|---------------------|
| mdl-card | Карточки статистики | Некорректное отображение, разъезжание layout |
| mdl-data-table | Таблица срочных дедлайнов | Проблемы стилизации, отсутствие данных |
| mdl-button | Кнопки управления | Неработающие обработчики событий |
| mdl-snackbar | Уведомления об ошибках | Функция showError не работает |
| mdl-layout | Общая структура страницы | Проблемы адаптивности, header |

##### Проблемы инициализации

**Возможные причины**:
- Скрипт `material.min.js` загружается после DOM
- Отсутствует вызов `componentHandler.upgradeDom()`
- Динамически создаваемые элементы не регистрируются в MDL

**Метод проверки**:
- Открыть консоль браузера и проверить наличие ошибок JavaScript
- Проверить порядок загрузки скриптов в dashboard.html
- Убедиться, что `defer` атрибут не блокирует инициализацию

#### 1.3 Взаимодействие с Backend API

##### CORS и сетевые запросы

**Конфигурация CORS** (backend/main.py, строки 65-71):

```
allow_origins: настройки из settings.cors_origins_list
allow_credentials: true
allow_methods: GET, POST, PUT, DELETE, OPTIONS
allow_headers: все (*)
```

**Проверяемые проблемы**:

| Проблема | Симптом | Решение |
|----------|---------|---------|
| CORS блокирует запросы | Ошибка в консоли "Access-Control-Allow-Origin" | Добавить `http://localhost:3000` в CORS_ORIGINS |
| Неправильный Content-Type | API возвращает 422 или 400 | Проверить заголовки запросов |
| Токен не передаётся | Все запросы возвращают 401 | Проверить формат заголовка Authorization |

##### Endpoint-ы для проверки

| Endpoint | Метод | Ожидаемый ответ | Текущая проблема |
|----------|-------|-----------------|------------------|
| `/api/auth/login` | POST | `{ "access_token": "...", "user": {...} }` | Проверить корректность аутентификации |
| `/api/dashboard/stats` | GET | Объект со статистикой | Проверить наличие всех полей |
| `/api/deadlines/urgent` | GET | Массив срочных дедлайнов | Endpoint может отсутствовать |
| `/api/deadline_types` | GET | Список типов дедлайнов | Для графика typeChart |
| `/health` | GET | `{ "status": "healthy" }` | Проверка доступности API |

#### 1.4 JavaScript-логика (dashboard.js)

##### Функции для тестирования

| Функция | Назначение | Проблема для проверки |
|---------|-----------|---------------------|
| `loadDashboardData()` | Загрузка всех данных дашборда | Неполная загрузка, отсутствующие вызовы API |
| `updateStatisticsCards()` | Обновление карточек статистики | Некорректное маппирование полей |
| `renderStatusChart()` | Отрисовка графика статусов | Ошибки Chart.js при нулевых значениях |
| `renderTypeChart()` | Отрисовка графика типов | Пустой массив данных |
| `renderUrgentDeadlines()` | Заполнение таблицы | Всегда пустой массив |
| `handleLogout()` | Выход из системы | Неполная очистка данных |

##### Потенциальные проблемы

**Константа API_BASE_URL** (строка 2):
- Жёстко закодировано `http://localhost:8000/api`
- Может не работать при другом хосте/порте

**Promise.all** (строка 36):
- Загружается только один endpoint `/dashboard/stats`
- Отсутствуют вызовы для срочных дедлайнов и типов

**Обработка ошибок**:
- Функция `showError()` использует MaterialSnackbar, который может быть не инициализирован
- Fallback на `alert()` может не срабатывать

### Стратегия тестирования Этапа 1

#### Подготовительные действия

1. Убедиться, что Backend API запущен и доступен на localhost:8000
2. Проверить наличие тестовых данных в базе данных (клиенты, дедлайны, типы)
3. Запустить веб-интерфейс на localhost:3000
4. Открыть консоль разработчика браузера для мониторинга ошибок

#### Методология тестирования

##### Функциональное тестирование

**Сценарий 1: Вход в систему**
1. Открыть `/login.html`
2. Ввести корректные учётные данные
3. Проверить наличие токена в localStorage
4. Проверить перенаправление на `/dashboard.html`

**Сценарий 2: Отображение статистики**
1. Загрузить dashboard
2. Проверить значения в карточках статистики
3. Сравнить с реальными данными из БД
4. Убедиться, что нет "-" вместо чисел

**Сценарий 3: Графики**
1. Проверить отображение statusChart
2. Проверить корректность данных и цветов
3. Проверить typeChart (ожидается ошибка - пустые данные)

**Сценарий 4: Таблица срочных дедлайнов**
1. Проверить содержимое таблицы
2. Если пусто - проверить сетевые запросы в DevTools
3. Найти отсутствующий вызов API

##### Интеграционное тестирование

**API + Frontend взаимодействие**:
- Проверить все запросы в Network tab
- Убедиться в корректности заголовков (Authorization, Content-Type)
- Проверить формат ответов API
- Убедиться в отсутствии CORS-ошибок

**Chart.js интеграция**:
- Проверить загрузку библиотеки Chart.js
- Проверить корректность создания canvas-элементов
- Протестировать с разными наборами данных (пустые, частичные, полные)

##### UI/UX тестирование

**Material Design Lite**:
- Проверить корректность отображения всех MDL-компонентов
- Проверить адаптивность (desktop, tablet, mobile)
- Проверить анимации и переходы
- Проверить доступность интерактивных элементов

**Кросс-браузерная совместимость**:
- Chrome/Edge (основной)
- Firefox
- Safari (если доступен)

#### Инструменты тестирования

| Инструмент | Назначение | Метод использования |
|-----------|-----------|---------------------|
| Browser DevTools | Анализ сетевых запросов, JavaScript-ошибок | Network tab, Console tab |
| SQLite Browser | Проверка данных в БД | Прямые SQL-запросы |
| Postman/curl | Тестирование API напрямую | Изолированная проверка endpoints |
| Python requests | Автоматизация API-тестов | Скрипты для массовых проверок |

### Категории дефектов для исправления

#### Критические (блокируют работу)

- Невозможность входа в систему
- Полное отсутствие данных на дашборде
- Ошибки JavaScript, блокирующие загрузку страницы
- CORS-блокировка всех запросов

#### Высокий приоритет (функциональность недоступна)

- Некорректные данные в статистике
- Неработающие графики
- Пустая таблица срочных дедлайнов
- Некорректная обработка истёкших токенов

#### Средний приоритет (частичная функциональность)

- Проблемы отображения MDL-компонентов
- Отсутствие данных в графике типов
- Неработающие уведомления об ошибках
- Проблемы адаптивности

#### Низкий приоритет (косметические)

- Неточности в форматировании дат
- Незначительные стилевые несоответствия
- Отсутствие loading-индикаторов

## Этап 2: Диагностика и исправление Telegram-бота

### Область диагностики

#### 2.1 Проблемы запуска бота

##### Типичные причины незапуска

**Проблемы конфигурации (.env файл)**

| Параметр | Обязательность | Типичная ошибка | Метод проверки |
|----------|---------------|-----------------|---------------|
| TELEGRAM_BOT_TOKEN | Обязательный | Токен не установлен или неверный формат | Валидация через bot/config.py |
| TELEGRAM_ADMIN_IDS | Обязательный | Пустое значение или неверный формат | Проверка парсинга списка |
| DATABASE_PATH | Обязательный | Неверный путь к БД | Проверка существования файла |
| WEB_API_BASE_URL | Обязательный для API-интеграции | Неверный URL или порт | Проверка доступности API |
| BOT_API_USERNAME | Для авторизации в API | Отсутствует или неверные credentials | Тест авторизации |
| BOT_API_PASSWORD | Для авторизации в API | Отсутствует или неверные credentials | Тест авторизации |

**Проверка конфигурации**:
- Запустить `python bot/config.py` для тестирования загрузки настроек
- Убедиться в отсутствии значений-заглушек типа `YOUR_BOT_TOKEN_FROM_BOTFATHER`
- Проверить формат токена: должен быть `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

##### Проблемы зависимостей Python

**Библиотеки для проверки**:

| Библиотека | Версия | Критичность | Типичная проблема |
|-----------|--------|-------------|------------------|
| aiogram | >= 3.2.0 | Критическая | Несовместимость версий, импорт не работает |
| aiohttp | >= 3.9.0 | Критическая | Требуется для aiogram |
| APScheduler | >= 3.10.0 | Высокая | Планировщик не запускается |
| sqlalchemy | >= 2.0.0 | Критическая | Несовместимость моделей БД |
| fastapi | >= 0.104.0 | Средняя | Для API-интеграции |

**Метод проверки**:
1. Запустить `pip list` и проверить установленные версии
2. Запустить `python bot/test_imports.py` (если существует)
3. Попробовать импортировать aiogram в Python-консоли

**Возможные ошибки импорта**:

```
ModuleNotFoundError: No module named 'aiogram'
ImportError: cannot import name 'DefaultBotProperties' from 'aiogram.client.default'
ImportError: No module named 'backend'
```

##### Проблемы путей и импортов

**Структура импортов в bot/main.py**:

```
from bot.config import bot_config
from bot.middlewares.auth import AuthMiddleware
from bot.handlers import common, admin, deadlines
from backend.database import SessionLocal
from backend.config import settings
```

**Потенциальные проблемы**:
- Бот запускается не из корневой директории проекта
- Python не видит пакет `backend` или `bot`
- Отсутствуют файлы `__init__.py` в необходимых директориях

**Метод проверки**:
- Убедиться, что запуск происходит из `d:\QoProj\KKT\`
- Проверить наличие всех `__init__.py` файлов
- Добавить текущую директорию в PYTHONPATH при необходимости

#### 2.2 Проблемы подключения к API

##### API Client инициализация

**Код инициализации** (bot/main.py, строки 67-99):

Функция `create_api_client()` создаёт WebAPIClient с TokenManager для автоматического обновления JWT-токенов.

**Проверяемые аспекты**:

| Компонент | Проблема | Симптом | Решение для проверки |
|-----------|----------|---------|---------------------|
| TokenManager | Не может получить токен | Ошибка при `token_manager.get_token()` | Проверить credentials в .env |
| WebAPIClient | Тайм-аут подключения | Ошибка "Web API недоступен" | Убедиться, что Backend API запущен |
| Dashboard Stats | Не может получить статистику | Exception при `get_dashboard_stats()` | Проверить endpoint `/api/dashboard/stats` |

**Тест подключения**:
- При успешном подключении в логах должно быть: `✅ Web API подключён успешно!`
- При неудаче: `⚠️ Web API недоступен (будет использован fallback)`

##### Fallback механизм

**Важно**: Бот должен запускаться даже при недоступности Web API, используя прямой доступ к БД через SQLAlchemy.

**Проверка fallback**:
- Запустить бот при выключенном Backend API
- Проверить, работают ли команды типа `/list`, `/today`
- Убедиться, что данные берутся напрямую из БД

#### 2.3 Проблемы базы данных

##### Подключение к SQLite

**Параметры подключения**:
- Путь к БД: из `settings.database_path`
- Создание сессий: через `SessionLocal()` из `backend.database`

**Типичные проблемы**:

| Проблема | Причина | Симптом |
|----------|---------|---------|
| База данных не найдена | Неверный путь в DATABASE_PATH | FileNotFoundError или OperationalError |
| База заблокирована | Открыта в другом процессе | sqlite3.OperationalError: database is locked |
| Несовместимая схема | Миграции не применены | Ошибки при выполнении запросов |
| Отсутствуют таблицы | БД не инициализирована | Table not found errors |

**Проверка БД**:
1. Запустить `python database/check_db.py`
2. Убедиться в наличии всех таблиц: users, deadlines, deadline_types
3. Проверить наличие тестовых данных

#### 2.4 Проблемы middleware и обработчиков

##### Middleware цепочка (bot/main.py, строки 102-121)

**Порядок middleware**:
1. LoggingMiddleware - логирование всех запросов
2. AuthMiddleware - добавление информации о роли пользователя

**Потенциальные проблемы**:
- Middleware не зарегистрирован для callback_query
- AuthMiddleware падает при отсутствии пользователя в БД
- Проблемы с созданием сессии БД внутри middleware

##### Регистрация обработчиков (bot/main.py, строки 124-161)

**Порядок регистрации роутеров**:

1. Команды (высокий приоритет)
   - common.router - `/start`, `/help`
   - admin.router - `/status`, `/check`, `/health`
   - deadlines.router - `/list`, `/today`, `/week`, `/next`
   - search.router - `/search`
   - settings_handler.router - `/settings`

2. CRUD команды (создают диалоги)
   - client_management.router - `/addclient`, `/editclient`, `/deleteclient`
   - deadline_management.router - `/adddeadline`, `/editdeadline`, `/deletedeadline`

3. Обработчик диалогов
   - crud_conversations.router - обработка текстовых сообщений

4. Экспорт
   - export.router - `/export` + callbacks

**Важно**: Порядок имеет значение! Обработчики с широкими фильтрами (F.text) должны быть последними.

**Проверка**:
- Убедиться, что все роутеры импортируются без ошибок
- Проверить, что нет конфликтов между обработчиками
- Тестировать команды по очереди

#### 2.5 Планировщик задач (Scheduler)

##### Инициализация (bot/main.py, строки 189-191)

**Компоненты**:
- Функция `setup_scheduler(bot, db_session, api_client)`
- Запуск через `scheduler.start()`
- Задача: проверка дедлайнов и отправка уведомлений

**Типичные проблемы**:

| Проблема | Причина | Метод диагностики |
|----------|---------|------------------|
| Планировщик не запускается | Ошибка в расписании (cron expression) | Проверить логи при запуске |
| Задача не выполняется | Неверная timezone | Проверить NOTIFICATION_TIMEZONE |
| Ошибки при выполнении задачи | Проблемы с БД или API | Логи выполнения задачи |

**Конфигурация из .env**:
- `NOTIFICATION_CHECK_TIME` - время проверки (например, "09:00")
- `NOTIFICATION_TIMEZONE` - часовой пояс (например, "Europe/Moscow")
- `NOTIFICATION_DAYS` - дни для уведомлений (например, "1,3,7,14")

#### 2.6 Логирование и диагностика

##### Конфигурация логов (bot/main.py, строки 32-39)

**Параметры**:
- Уровень: INFO
- Файл: `logs/bot.log` (с кодировкой UTF-8)
- Также вывод в консоль (StreamHandler)

**Проверка**:
1. Убедиться, что директория `logs/` существует (создаётся автоматически)
2. Проверить наличие файла `logs/bot.log` после запуска
3. Анализировать последние записи на предмет ошибок

**Ключевые сообщения в логах**:

| Сообщение | Значение | Этап запуска |
|-----------|----------|-------------|
| 🚀 ЗАПУСК TELEGRAM БОТА ККТ | Начало инициализации | Старт |
| 🔌 Инициализация Web API клиента... | Подключение к API | Инициализация API |
| ✅ Web API подключён успешно! | API доступен | Проверка API |
| ✅ Middleware зарегистрированы | Middleware настроены | Настройка middleware |
| ✅ Обработчики команд зарегистрированы | Роутеры подключены | Регистрация handlers |
| ✅ Планировщик запущен | Scheduler активен | Запуск scheduler |
| 🤖 Бот запущен: @username | Бот готов к работе | Успешный запуск |
| ✅ Бот готов к работе! | Polling начат | Готовность |

**Диагностика по логам**:
- Найти последнее успешное сообщение
- Проверить наличие ошибок или warning после него
- Особое внимание на ошибки импорта, конфигурации, подключения к БД/API

### Стратегия тестирования Этапа 2

#### Подготовительные действия

1. Убедиться, что Backend API запущен и доступен
2. Проверить наличие и корректность файла `.env`
3. Убедиться, что база данных инициализирована и содержит данные
4. Установить/обновить все зависимости из requirements.txt

#### Методология диагностики

##### Шаг 1: Попытка запуска

**Команда запуска**:
```
Запуск из корневой директории проекта
Windows: python -m bot.main
Linux/Mac: python3 -m bot.main
Альтернатива: python bot/main.py
```

**Ожидаемые сценарии**:

1. **Бот не запускается вообще** (ImportError, ModuleNotFoundError)
   - Проблема: Зависимости или импорты
   - Действие: Проверить установку пакетов, структуру проекта

2. **Бот запускается, но падает с ошибкой**
   - Проблема: Конфигурация, БД, API
   - Действие: Анализ traceback, проверка логов

3. **Бот запускается, но не отвечает на команды**
   - Проблема: Обработчики, middleware
   - Действие: Тестирование команд, проверка регистрации роутеров

4. **Бот работает частично**
   - Проблема: Отдельные функции или интеграции
   - Действие: Изолированное тестирование функций

##### Шаг 2: Тестирование конфигурации

**Тест 1: Проверка bot/config.py**
```
Запустить: python bot/config.py
Ожидаемый вывод: Информация о конфигурации бота без ошибок
```

**Тест 2: Проверка backend/config.py**
```
Запустить: python -c "from backend.config import settings; print(settings.telegram_bot_token[:20])"
Ожидаемый вывод: Первые 20 символов токена
```

**Тест 3: Валидация .env файла**
- Проверить наличие всех обязательных параметров
- Убедиться в отсутствии комментариев в значениях
- Проверить формат списков (через запятую)

##### Шаг 3: Тестирование зависимостей

**Тест импортов**:
```
Python-команды для проверки:
import aiogram
import aiohttp
import apscheduler
from bot.config import bot_config
from backend.database import SessionLocal
```

**При ошибках**:
- Переустановить зависимости: `pip install -r requirements.txt --force-reinstall`
- Проверить версию Python: должна быть >= 3.6
- Убедиться в использовании правильного venv

##### Шаг 4: Тестирование подключения к БД

**Запустить диагностический скрипт**:
```
python database/check_db.py
или
python -c "from backend.database import check_db_connection; print(check_db_connection())"
```

**Ожидаемый результат**: True (база данных доступна)

**При ошибке**:
- Проверить путь DATABASE_PATH в .env
- Убедиться, что файл БД существует
- Проверить права доступа к файлу

##### Шаг 5: Тестирование API-интеграции

**Тест доступности Backend API**:
```
curl http://localhost:8000/health
или
python -c "import requests; print(requests.get('http://localhost:8000/health').json())"
```

**Тест авторизации бота в API**:
```
Проверить, что BOT_API_USERNAME и BOT_API_PASSWORD корректны
Проверить, что пользователь существует в БД
Проверить, что роль пользователя позволяет доступ к API
```

##### Шаг 6: Пошаговый запуск с логированием

**Добавление дополнительного логирования**:
- Установить уровень DEBUG в bot/main.py
- Добавить print-отладку в критичных местах
- Запустить бота и анализировать вывод построчно

**Ключевые точки для проверки**:
1. Создание экземпляра бота (строка 173)
2. Создание диспетчера (строка 174)
3. Создание сессии БД (строка 175)
4. Инициализация API клиента (строка 178)
5. Настройка middleware (строка 185)
6. Регистрация обработчиков (строка 186)
7. Запуск планировщика (строки 189-191)
8. Получение информации о боте (строка 195)
9. Запуск polling (строки 217-221)

#### Функциональное тестирование бота

После успешного запуска необходимо протестировать функциональность:

##### Группа 1: Базовые команды

| Команда | Ожидаемый результат | Проверяемая функциональность |
|---------|-------------------|----------------------------|
| `/start` | Приветственное сообщение с инструкциями | Базовая работа бота |
| `/help` | Список доступных команд | Корректность меню помощи |

##### Группа 2: Команды просмотра

| Команда | Ожидаемый результат | Проверяемая интеграция |
|---------|-------------------|----------------------|
| `/list` | Список всех дедлайнов | БД + форматирование |
| `/today` | Дедлайны на сегодня | Фильтрация по дате |
| `/week` | Дедлайны на неделю | Расчёт диапазона дат |
| `/next` | Ближайшие дедлайны | Сортировка и лимит |

##### Группа 3: Административные команды

| Команда | Требования | Ожидаемый результат |
|---------|-----------|-------------------|
| `/status` | Пользователь в TELEGRAM_ADMIN_IDS | Статистика системы |
| `/check` | Админ | Информация о дедлайнах |
| `/health` | Админ | Состояние БД и API |

##### Группа 4: CRUD операции

**Клиенты**:
- `/addclient` - начало диалога добавления клиента
- `/editclient` - редактирование существующего клиента
- `/deleteclient` - удаление клиента

**Дедлайны**:
- `/adddeadline` - добавление нового дедлайна
- `/editdeadline` - редактирование дедлайна
- `/deletedeadline` - удаление дедлайна

**Проверка диалогов**:
- Корректность FSM (Finite State Machine)
- Обработка текстовых ответов пользователя
- Валидация введённых данных
- Сохранение в БД
- Обработка отмены операции

##### Группа 5: Дополнительные функции

| Функция | Команда/триггер | Проверка |
|---------|----------------|---------|
| Поиск | `/search <запрос>` | Поиск по клиентам и дедлайнам |
| Экспорт | `/export` | Генерация JSON-файла с данными |
| Настройки | `/settings` | Персональные настройки уведомлений |

#### Тестирование планировщика

**Проверка автоматических уведомлений**:

1. Настроить NOTIFICATION_CHECK_TIME на ближайшее время (например, через 2 минуты)
2. Убедиться, что в БД есть дедлайны, попадающие в NOTIFICATION_DAYS
3. Дождаться срабатывания планировщика
4. Проверить, что уведомления отправлены администраторам

**Альтернатива - ручной триггер**:
- Если есть команда `/check`, использовать её для немедленной проверки
- Проверить логи планировщика на наличие ошибок

### Категории дефектов для исправления (Бот)

#### Критические (бот не работает)

- Невозможность запуска бота (ImportError, ConfigError)
- Бот не подключается к Telegram API (неверный токен)
- Бот не может подключиться к БД
- Падение при обработке любой команды

#### Высокий приоритет (основная функциональность недоступна)

- Не работает аутентификация пользователей
- Команды просмотра дедлайнов возвращают ошибки
- CRUD операции не сохраняют данные
- API-интеграция не работает (при выключенном fallback)
- Планировщик не отправляет уведомления

#### Средний приоритет (частичная функциональность)

- Некорректное форматирование сообщений (кривая вёрстка)
- Проблемы с FSM в диалогах (зависание состояния)
- Middleware не логирует запросы
- Некорректная обработка ошибок (нет user-friendly сообщений)

#### Низкий приоритет (косметические)

- Опечатки в текстах сообщений
- Неоптимальное форматирование
- Отсутствие emoji в сообщениях

## Общая стратегия исправления

### Приоритизация работ

1. **Критические дефекты** - исправление немедленно, блокирует дальнейшее тестирование
2. **Высокий приоритет** - исправление в первую очередь, основная функциональность
3. **Средний приоритет** - исправление после основных функций
4. **Низкий приоритет** - исправление при наличии времени

### Процесс исправления

#### Для каждого дефекта

1. **Идентификация**
   - Точное описание проблемы
   - Шаги воспроизведения
   - Ожидаемое vs фактическое поведение

2. **Анализ причины**
   - Изучение кода
   - Анализ логов
   - Проверка конфигурации
   - Изучение сетевых запросов (для API)

3. **Определение решения**
   - Выбор оптимального подхода
   - Оценка влияния на другие компоненты
   - Проверка совместимости

4. **Реализация исправления**
   - Внесение изменений в код
   - Соблюдение стандартов кодирования
   - Добавление комментариев при необходимости

5. **Тестирование исправления**
   - Проверка устранения проблемы
   - Регрессионное тестирование (не сломалось ли что-то ещё)
   - Проверка граничных случаев

6. **Документирование**
   - Описание проблемы
   - Описание решения
   - Обновление документации при необходимости

### Требования к качеству исправлений

#### Код

- Соответствие существующему стилю кода
- Понятные имена переменных и функций
- Комментарии для сложной логики
- Отсутствие дублирования кода

#### Функциональность

- Исправление устраняет проблему полностью
- Не возникает новых проблем (регрессия)
- Работает во всех предусмотренных сценариях
- Корректная обработка ошибок

#### Совместимость

- Не нарушается работа других компонентов
- Соблюдается обратная совместимость (если применимо)
- Корректная работа в разных браузерах (для фронтэнда)

#### Документация

- Все изменения задокументированы
- Обновлены README и инструкции при необходимости
- Добавлены комментарии в код где требуется

### Отчётность

#### Формат отчёта о дефекте

| Поле | Описание |
|------|----------|
| ID | Уникальный идентификатор |
| Компонент | Frontend / Backend / Bot / API Integration |
| Приоритет | Критический / Высокий / Средний / Низкий |
| Описание | Что работает неправильно |
| Шаги воспроизведения | Как воспроизвести проблему |
| Ожидаемый результат | Что должно быть |
| Фактический результат | Что происходит на самом деле |
| Логи/Скриншоты | Дополнительная информация |
| Причина | Найденная root cause |
| Решение | Описание исправления |
| Статус | Открыт / В работе / Исправлен / Проверен |

#### Итоговый отчёт

После завершения всех работ должен быть сформирован итоговый отчёт:

- Общее количество найденных дефектов
- Разбивка по приоритетам
- Разбивка по компонентам
- Список исправленных проблем
- Список нерешённых проблем (если есть) с обоснованием
- Рекомендации по дальнейшей разработке

## Критерии завершения задачи

### Этап 1: Frontend/CoreUI

**Успешное завершение**:
- Веб-интерфейс корректно отображается во всех поддерживаемых браузерах
- Все карточки статистики показывают корректные данные из БД
- График statusChart отображается корректно с правильными данными
- График typeChart отображается с данными (если endpoint реализован)
- Таблица срочных дедлайнов заполняется данными из API
- Все MDL-компоненты работают корректно
- Отсутствуют ошибки JavaScript в консоли
- Отсутствуют CORS-ошибки
- Авторизация работает стабильно
- Корректная обработка истёкших токенов

### Этап 2: Telegram Bot

**Успешное завершение**:
- Бот запускается без ошибок
- Все основные команды работают корректно
- CRUD операции сохраняют данные в БД
- Интеграция с Backend API работает (или fallback на БД)
- Планировщик отправляет уведомления по расписанию
- Middleware корректно обрабатывает запросы
- Логирование работает, логи информативны
- Отсутствуют критические ошибки в работе

### Общие критерии качества

- Код соответствует стандартам проекта
- Отсутствуют хардкод-значения где их не должно быть
- Все компоненты работают стабильно
- Система готова к продуктивному использованию
- Документация обновлена (при необходимости)

## Технические ограничения и предположения

### Предположения

- Backend API доступен и работает корректно
- База данных инициализирована и содержит корректную схему
- Используется Python 3.6+ для бота и бэкенда
- Используется современный браузер для веб-интерфейса (Chrome/Firefox/Edge)

### Ограничения

- Не изменяется архитектура системы
- Не производится рефакторинг без крайней необходимости
- Сохраняется существующий стек технологий
- Русский язык для всех пользовательских интерфейсов (согласно спецификации проекта)

### Риски

| Риск | Вероятность | Влияние | Митигация |
|------|------------|---------|-----------|
| Критические дефекты в коде, требующие значительного рефакторинга | Средняя | Высокое | Тщательный анализ перед исправлением |
| Несовместимость версий библиотек | Низкая | Среднее | Фиксация версий в requirements.txt |
| Проблемы с БД, требующие миграций | Средняя | Высокое | Резервное копирование перед изменениями |
| Неполная документация существующего кода | Высокая | Среднее | Изучение кода, добавление комментариев |

## Дополнительные соображения

### Тестовые данные

Для полноценного тестирования необходимо наличие в БД:

- Минимум 5-10 клиентов с разными статусами
- Минимум 15-20 дедлайнов с разными датами истечения
  - Просроченные (прошедшие даты)
  - Срочные (0-7 дней)
  - Требующие внимания (7-14 дней)
  - Нормальные (> 14 дней)
- Различные типы дедлайнов
- Тестовый пользователь с правами администратора

### Инструменты разработки

Рекомендуемые инструменты для работы:

- **IDE**: VS Code, PyCharm
- **Браузер**: Chrome/Edge с DevTools
- **API тестирование**: Postman, curl, httpie
- **БД просмотр**: DB Browser for SQLite
- **Логирование**: Консоль + файлы логов
- **Git**: Для контроля версий и отката изменений

### Порядок работы

**Рекомендуемая последовательность**:

1. Начать с Этапа 2 (Telegram Bot)
   - Причина: Бот критичен для уведомлений, более приоритетен
   - Проще диагностировать (консольный вывод, логи)

2. Перейти к Этапу 1 (Frontend/CoreUI)
   - После исправления бота можно проверить интеграцию
   - Веб-интерфейс зависит от работы Backend API

3. Финальная интеграционная проверка
   - Протестировать все компоненты вместе
   - Проверить сквозные сценарии (создание клиента через веб → уведомление через бота)

### Дополнительные проверки

После исправления всех найденных дефектов рекомендуется:

- Запустить все компоненты одновременно и проверить работу системы в целом
- Провести нагрузочное тестирование (создание большого количества дедлайнов)
- Проверить работу при различных сетевых условиях (медленное соединение, таймауты)
- Проверить работу при сбоях компонентов (выключение API, недоступность БД)

## Зависимости от других компонентов

### Frontend зависит от

- Backend API (все данные)
- Правильной конфигурации CORS
- Наличия валидного JWT токена
- Корректной работы endpoints

### Telegram Bot зависит от

- Backend API (опционально, есть fallback)
- База данных SQLite (обязательно)
- Telegram Bot API (обязательно)
- Корректной конфигурации в .env

### Взаимные зависимости

- И Frontend, и Bot используют одну БД
- Изменения через Frontend должны быть видны в Bot и наоборот
- Backend API должен корректно обрабатывать запросы от обоих клиентов
